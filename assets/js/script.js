// global variables
var startTime = 30;
var time = startTime;
var timer = getElementById("timer");

// functions
    // timer function

setInterval(function() {
    time--
    console.log(timer)
    timer.innerText = time
}, 1000)
// question array